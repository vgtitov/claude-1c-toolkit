# Архитектурный аудит onec_metadata (2026-07)

Проверка: действительно ли наш подход к программному редактированию метаданных
1С — лучший из доступных. Сравнение с отраслью и передовыми методами; выводы и
задачи. Обезличено.

## Наши ключевые решения

1. Манипуляция ИСХОДНЫМИ файлами выгрузки (Конфигуратор XML + EDT `.mdo`) через
   lxml.
2. Паттерн «deepcopy реального элемента-образца + подстановка», без моделирования
   матрицы свойств по версии/совместимости.
3. byte-perfect минимальный дифф.
4. Применение батч-DESIGNER в ТЕСТ-базу + round-trip verify как гейт (платформа =
   источник истины).
5. Оба формата с автоопределением.
6. Реестр покрытия `catalog.SUPPORTED`.

## Вердикт: подход обоснован и совпадает с best practice по фундаменту

Подтверждено (со ссылками — в задаче-исследователе, ниже тезисно):

- **File-first оправдан фундаментально.** Встроенный язык 1С даёт доступ к
  метаданным ТОЛЬКО на чтение; создавать/менять объекты можно лишь в дизайн-тайме
  (Конфигуратор/EDT) или правкой исходных файлов + загрузкой платформой (ITS
  metod8dev/2318). Рантайм-API создания метаданных не существует — значит наш путь
  (правка файлов, headless, без Java/Eclipse) — самый автоматизируемый из легальных.
- **«Платформа = источник истины» + round-trip verify** — ровно то, что делает
  весь git-workflow отрасли (gitsync, 1С:GitConverter): человек/инструмент меняет
  → платформа выгружает/проверяет.
- **deepcopy образца, а не сборка с нуля** — совпадает с отраслевой утилитой
  программного создания ролей (infostart 2099929) и обосновано тем, что порядок
  свойств и версия сериализации официально НЕ документированы.
- **byte-perfect минимальный дифф — НАШЕ преимущество.** Отраслевой пайплайн
  (gitsync) полагается на платформенную перевыгрузку и чистого минимального диффа
  ввода не даёт. Наш чистый PR-дифф — плюс к ревью, которого у отрасли нет.

## Проверено запуском (наши факты)

- `set-property` корректно правит реальный справочник УТ (реальные флаги,
  byte-perfect, BOM+CRLF). Text-подход на реальных данных держится.
- Платформа НЕ поставляет XSD-схемы формата в `bin` → дешёвой офлайн-валидации
  «по схеме» нет; round-trip через платформу остаётся основным гейтом.
- Реестр `catalog.SUPPORTED` НЕ энфорсится в рантайме (используется только в
  `catalog.py` и тестах) — заявленный в CLAUDE.md «предохранитель» пока
  документационный.

## Где отрасль сильнее и что перенять

1. **Каскадный рефакторинг (главное отставание).** Передовые EDT-плагин-MCP
   (DitriXNew/EDT-MCP, MCP:RSV) правят метаданные через внутреннюю EMF-модель EDT
   и при переименовании/удалении объекта чинят ссылки во ВСЁМ проекте (BSL +
   формы + метаданные). Наша text-манипуляция этого не даёт даром.
   **Решение (архитектурная граница, см. ниже):** наш инструмент сознательно
   ограничивается БЕЗОПАСНЫМИ аддитивными и скалярными правками; деструктив с
   каскадом (rename/delete/move) — вне scope, отдаётся EDT-плагину или человеку.
2. **Пересчёт ConfigDumpInfo платформой.** Финальный артефакт для git должен быть
   платформенно-нормализован (версии/порядок/ConfigDumpInfo — платформой, не
   руками). Наш round-trip уже выгружает платформенное дерево — надо явно
   зафиксировать, что коммитим ИМЕННО его, а не пред-apply правку.
3. **`1cedtcli` как стандартный конвертер/валидатор** для `.mdo` (headless, но
   тянет Java/Eclipse) — оценить, не дешевле ли он самодельных обёрток для
   EDT-формата.

## Архитектурная граница (ключевое решение аудита)

Инструмент делает и будет делать ТОЛЬКО правки, безопасные без анализа ссылок:

- **В scope (безопасно):** добавить реквизит/ТЧ/команду/измерение/ресурс/значение,
  колонку печатной формы, поле/запрос СКД, включить в подсистему, выдать право,
  изменить существующее СКАЛЯРНОЕ свойство. Ни одна из них не ломает внешние ссылки.
- **Вне scope (нужен каскад):** переименование, удаление, перемещение объекта,
  смена вида объекта. Требуют правки ссылок во всём проекте — это надёжно делает
  только EDT internal-model (плагин), не text-манипуляция. Такие операции —
  человеку в IDE или через EDT-плагин-MCP.

Это не слабость, а осознанная граница: аддитивные+скалярные правки закрывают
подавляющее большинство рутинных доработок с гарантией round-trip, а рискованный
каскад не имитируется вручную.

## Углублённое исследование EDT-коннектора для каскада (решение)

Вопрос: переиспользовать модель 1C:EDT для каскадного rename/delete (починка
ссылок в BSL/формах/метаданных) — вместо имитации текстом, и «без привязки к
версиям». Разобрано по исходникам DitriXNew/EDT-MCP и офиц. докам EDT.

- **Каскад реален только через штатный LTK-рефакторинг EDT** (`IMdRefactoringService`,
  `com._1c.g5.v8.dt.md.refactoring.core`), запускаемый из OSGi-плагина ВНУТРИ (в т.ч.
  headless) EDT. DitriX не пишет каскад руками — делегирует движку EDT, тот считает
  ChangeSet для BSL+форм+метаданных. Через `1cedtcli`/файлы каскада не получить.
- **«Без привязки к версиям» — миф.** Меняем зависимость от формата `.mdo` на
  зависимость от внутреннего Java-API EDT (версионируется по major; DitriX держит
  диапазоны бандлов и рефлексию для хрупких мест) + от установленной EDT. DitriX
  заявлен только под EDT 2026.1. Это перенос ЕДИНСТВЕННОЙ точки привязки на EDT SDK,
  а не свобода.
- **Стоимость своего плагина:** код каскада мал (у DitriX готов), дорого —
  инфраструктура: живая EDT, тяжёлый headless-запуск под операцию (JVM+индекс),
  чужой Tycho/OSGi-тулчейн, ВЕЧНОЕ сопровождение под каждый релиз EDT.
- **Лицензии:** DitriX — AGPL v3 (внутреннее использование свободно; форк/дистрибуция
  обязывает открывать изменения). RSV — закрытый платный (только делегировать).

**Решение аудита:** свой EDT-плагин с нуля НЕ строить. Наш лёгкий lxml-механизм —
для безопасных аддитивных/скалярных правок (там он проще и сильнее). Каскад
(rename/delete/move) по мере надобности:
1. **редко** → человек в EDT (диалог рефакторинга с preview/откатом) — нулевой техдолг;
2. **нужна автоматизация** → делегировать готовому **DitriX EDT-MCP headless** как
   «сервису каскада» (двухфазно, с preview); AGPL для внутреннего использования не мешает,
   форкать без нужды не стоит.
Свой тонкий плагин оправдан лишь в узком пересечении (AGPL мешает поставке И каскад
нужен массово-автоматизированным И готовы платить налог сопровождения). Иначе — 1 или 2.

**Прототип варианта 2 ПРОВЕРЕН ЖИВЬЁМ 2026-07-14 на EDT 2025.2.6** (заявленная
автором совместимость — 2026.1, но): p2 director ставит готовый update site
(`ditrixnew.github.io/EDT-MCP`, feature 2.6.1) в 2025.2.6 без конфликтов
зависимостей; headless-запуск EDT с `EDT_MCP_AUTO_START=true` +
`EDT_MCP_IMPORT_PROJECTS` поднимает `/health` за ~90 с (`ready:true`,
missingServices пусто); 76 инструментов; каскадный
`rename_metadata_object` двухфазно (preview: таблица change points с BSL-контекстом
→ `confirm=true`) переименовал общий модуль И обновил вызов в ЧУЖОМ модуле
(`CascadeUser.bsl`). Нюансы: запись на диск после rename асинхронна (flush
отстаёт от ответа executed — перед git-операциями дождаться/остановить EDT);
для автоматизации деструктива нужен `EDT_MCP_DESTRUCTIVE_CONSENT=allow` на
процессе EDT.

Итог: подтверждает нашу scope-границу. Каскад — не имитировать текстом (риск как с
дублированием UUID у ТЧ, который только что выловили), а отдавать EDT.

## Данных не хватило (не выдумываем)

- Тезис «EDT `.mdo` опускает значения по умолчанию» надёжного открытого источника
  не получил — **проверен эмпирически 2026-07-14** на реальной EDT-выгрузке ERP 2.5
  (9382 `.mdo`, 25255 реквизитов): по 11 замеренным свойствам НИ ОДНОГО вхождения
  с дефолтным значением; свойство отсутствует ⇔ значение дефолтно (пример:
  `fillChecking` есть у 8153 реквизитов — все `ShowError`; у остальных ~17k опущено
  = `DontCheck`). Нюанс: дефолт = дефолт EMF-модели EDT, НЕ дефолт UI Конфигуратора
  (`fullTextSearch`: EDT-дефолт `DontUse`, поэтому `Use` пишется явно — 35281 раз).
- Канонической спецификации порядка свойств выгрузки Конфигуратора в открытом
  доступе нет (формат неофициален) — это аргумент ЗА «копируй образец».

## Задачи (заведены в BACKLOG.md, раздел «Аудит 2026-07»)

- Зафиксировать scope-границу в CLAUDE.md/README (сделано этим аудитом).
- Пересчёт ConfigDumpInfo/канонизация артефакта платформой в apply-гейте.
- Решение по `catalog.SUPPORTED`: рантайм-энфорс ИЛИ переименовать в «tested
  coverage» и убрать формулировку «предохранитель» из CLAUDE.md.
- Оценить `1cedtcli` как конвертер/валидатор для EDT.
- ~~Эмпирически проверить поведение `.mdo` с дефолтами~~ — сделано 2026-07-14 (см. выше).
