// aidbg_API - тонкие обработчики HTTP-сервиса: вся логика в aidbg_Сервер.
// Любое необработанное исключение превращается в JSON-ответ 500 (без стека наружу).

Функция queryPOST(Запрос)

	Попытка
		Возврат aidbg_Сервер.ОбработатьQuery(Запрос);
	Исключение
		Возврат aidbg_Сервер.ОтветВнутренняяОшибка(ИнформацияОбОшибке());
	КонецПопытки;

КонецФункции

Функция reportPOST(Запрос)

	Попытка
		Возврат aidbg_Сервер.ОбработатьReport(Запрос);
	Исключение
		Возврат aidbg_Сервер.ОтветВнутренняяОшибка(ИнформацияОбОшибке());
	КонецПопытки;

КонецФункции

Функция settingsPOST(Запрос)

	Попытка
		Возврат aidbg_Сервер.ОбработатьSettings(Запрос);
	Исключение
		Возврат aidbg_Сервер.ОтветВнутренняяОшибка(ИнформацияОбОшибке());
	КонецПопытки;

КонецФункции

Функция callPOST(Запрос)

	Попытка
		Возврат aidbg_Сервер.ОбработатьCall(Запрос);
	Исключение
		Возврат aidbg_Сервер.ОтветВнутренняяОшибка(ИнформацияОбОшибке());
	КонецПопытки;

КонецФункции

Функция healthGET(Запрос)

	Попытка
		Возврат aidbg_Сервер.ОбработатьHealth(Запрос);
	Исключение
		Возврат aidbg_Сервер.ОтветВнутренняяОшибка(ИнформацияОбОшибке());
	КонецПопытки;

КонецФункции
